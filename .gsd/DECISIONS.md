# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Starting point | GSD-2 codebase, not greenfield | GSD-2 has all the hard infrastructure (crash recovery, cost tracking, timeout, multi-provider LLM, state machine). Research logic is the new part. | No |
| D002 | M001 | arch | Hierarchy mapping | Campaign=Milestone, Phase=Slice, Experiment=Task | Inherit GSD-2's existing structure rather than inventing new abstractions. Same state machine, same file structure, same dispatch. | No |
| D003 | M001 | arch | Research flow semantics | Failure is data, phases are theories, advancement is exploration-driven | Fundamental difference from development flow. Failed experiments advance with knowledge, not block. | No |
| D004 | M001 | arch | MLOps integration strategy | Two-layer: Labrat logs orchestration via REST, eval scripts log domain metrics natively | User's eval scripts already live in Python where W&B/MLFlow SDKs are native. Labrat adds orchestration metadata via REST API. | Yes — if a TypeScript W&B SDK appears |
| D005 | M001 | pattern | Eval output format | JSON to stdout | Simple, parseable, language-agnostic. Eval prints {"metric": value} and Labrat parses it. | No |
| D006 | M001 | scope | Target file safety | Agent can only modify files explicitly specified by user | Eval script and infrastructure are immutable. Safety boundary prevents unintended modifications. | Yes — when multi-file experiments added in M002 |
| D007 | M001 | arch | Integration philosophy | Integrate existing tools, don't rebuild | Anything with a mature existing solution (MLOps tracking, visualization, statistical analysis) gets wired in, not reimplemented. Labrat's unique value is the autonomous research loop. | No |
| D008 | M001 | scope | LLM provider support | Keep full GSD-2 multi-provider support (20+) | Zero cost to maintain, inherited infrastructure. Users pick whatever model suits their research. | No |
| D009 | M001 | arch | Upstream tracking | Cherry-pick selective, not merge | Codebases will diverge. Cherry-pick infrastructure fixes from GSD-2, ignore development-specific changes. | No |
| D010 | M001/S01 | pattern | Integration branch | `dev` branch as integration target, not `main` | Slice branches branch from and merge into `dev`. `main` stays clean for releases. | No |
| D011 | M001/S01 | scope | Internal package naming | Keep `@gsd/*` workspace names and `gsd` extension directory unchanged | Renaming cascades into every import across 5 workspace packages for zero user-facing benefit. User-facing identity (package name, bin, env vars, config dir) is `labrat`. | Yes — if internal names cause confusion |
| D012 | M001/S02 | pattern | Campaign config format | `CAMPAIGN.json` in slice directory | JSON is parseable without markdown parsing complexity. Lives in slice directory because campaigns map to slices (D002). JSONL experiment log alongside it. | Yes — could switch to CAMPAIGN.md if markdown consistency matters |
| D013 | M001/S02 | pattern | Experiment log format | JSONL (one JSON object per line) | Crash-safe: truncated last line is recoverable. Append-only via appendFileSync. More robust than a single JSON array for crash scenarios. | No |
| D014 | M001/S02 | pattern | Experiment commit convention | `experiment(E001): description` / `revert(E001): discard — reason` | Greppable, follows conventional commit pattern. Distinct from development `chore()` commits. Enables git log filtering for experiment history. | No |
| D015 | M001/S02 | arch | Dual-mode state machine | Parallel research mode alongside existing development flow | `experimenting` phase added to Phase union, not replacing existing phases. Existing development flow untouched. Campaign config presence triggers research mode in deriveState(). | No |
| D016 | M001/S02/T01 | pattern | Campaign detection priority | Campaign config short-circuits deriveState before blocker detection | Active campaign takes priority over task-level blocker detection. Rationale: experiments are the work unit in research mode, not tasks. | Yes — may need refinement when experiments interact with task blockers |
| D017 | M001/S02/T01 | pattern | run-experiment artifact verification | Artifact path points to EXPERIMENT-LOG.jsonl existence, not individual entries | Auto-mode's retry/skip logic checks file existence. Entry-level validation is the eval runner's job (S03). | Yes — could add entry count check later |
| D018 | M001/S02/T02 | pattern | Dispatch guard for experiments | run-experiment added to SLICE_DISPATCH_TYPES (same sequential check as other slice work) | Experiments follow the same ordering constraint — can't dispatch until prior slices complete on main. Simpler than a custom bypass. | Yes — may need experiment-specific bypass if campaigns span multiple slices |
| D019 | M001/S02/T02 | pattern | commitExperiment failure mode | Throws on empty staging instead of silent no-op | Caller must know when nothing was committed. Silent no-ops hide bugs in experiment generation. | No |
| D020 | M001/S03/T01 | pattern | Metric parsing strategy | Bottom-up JSON scanning — last valid JSON object line wins | Eval tools commonly print progress/warnings then final metrics. Bottom-up finds the metrics line without requiring the eval to suppress other output. | No |
| D021 | M001/S03/T01 | pattern | Multi-run aggregation | Median per metric, not mean | Median is robust against outlier runs (one slow/fast run doesn't skew results). Standard practice for noisy benchmarks. | No |
| D022 | M001/S03/T01 | pattern | Zero baseline guard | Use divisor of 1 when baseline metric is 0, not epsilon | Epsilon (1e-10) produces astronomically large normalized scores that dominate composite scoring. Divisor of 1 produces meaningful relative scores. | No |
| D023 | M001/S03/T02 | pattern | Eval hook placement in handleAgentEnd | Between auto-commit and doctor/state-rebuild | Eval sees the committed code (post-commit). Doctor runs after any revert (post-eval). Correct ordering for both keep and discard paths. | No |
| D024 | M001/S03 | process | Do NOT write STATE.md in completion units | Skip STATE.md in slice completion — let auto-mode bookkeeping own that file | Auto-mode modifies STATE.md between auto-commit and branch checkout during squash-merge. If the completion unit also writes STATE.md, the file has uncommitted changes at checkout time and git aborts with "Your local changes would be overwritten." Broke the S03 merge. Completion units must not touch STATE.md. | No |
| D025 | M001/S04 | pattern | Missing target file degradation | Insert ⚠ placeholder + stderr warning instead of throwing | A missing file shouldn't crash an overnight run. LLM can still attempt modifications with remaining context. Consistent with resilient-by-default patterns (readAllExperiments skips corrupted lines, readBestMetrics returns null). | No |
| D026 | M001/S04 | pattern | Diff-stat timing in eval post-process | Extract diff-stat before any potential git revert | Revert changes HEAD, so `git diff --stat HEAD~1..HEAD` must be captured first. Applied to all three result construction sites in runExperimentPostProcess(). | No |
| D027 | M001/S05 | pattern | Lock enrichment strategy | Read-modify-write after writeLock, not changing writeLock signature | Preserves backward compatibility for all existing lock consumers. Only experiment dispatch path adds experimentNumber. | No |
| D028 | M001/S05 | pattern | Orphan commit detection | Git log --grep vs JSONL entry count | Relies on D014 commit message convention (`experiment(EYYY):`). Simple count comparison — more commits than log entries means orphan. | Yes — if commit convention changes |
| D029 | M001/S05 | pattern | Per-experiment budget comparison | Strict > for per-experiment, >= for campaign-level | At-budget is acceptable for a single experiment (noise margin). At-ceiling for the whole campaign is a hard stop. Different semantics, different operators. | Yes |
| D030 | M001/S05 | pattern | Budget guard failure semantics | Non-fatal try/catch — budget check failure never blocks dispatch | Budget is a safety valve, not a gate. If the check itself fails (ledger unavailable, format change), experiments continue rather than halting. | No |
| D031 | M001/S06 | pattern | MLOps circuit breaker | 5 consecutive failures → 5 min backoff, half-open probe, injectable nowFn for testing | Prevents hammering a down MLOps server while allowing automatic recovery. Constructor-injected for testability. | Yes — thresholds may need tuning |
| D032 | M001/S06 | pattern | Non-fatal MLOps hooks | All three lifecycle points (init/log/finish) wrapped in try/catch — MLOps failure never blocks experiments | MLOps is observability, not a gate. An overnight run must never fail because W&B is down. | No |
| D033 | M001/S06 | pattern | W&B auth encoding | Buffer.from base64 of "api:<key>" matching SDK wire protocol | Reverse-engineered from W&B Python SDK behavior. Undocumented but stable. | Yes — if W&B changes auth |
| D034 | M001/S07 | pattern | `labrat start` auto-trigger mechanism | Env var `LABRAT_AUTO_START=1` checked by session_start hook in GSD extension | Simple, ephemeral (process-scoped), no new APIs. Extension's session_start handler already has `pi` in scope for calling startAuto(). | Yes — could use a more formal hook if needed |
| D035 | M001/S07 | pattern | `labrat start` GSD scaffold | Creates minimal M001/S01 with roadmap, plan, and CAMPAIGN.json — just enough for deriveState() | deriveState() requires slice plan to be truthy before campaign detection fires (line 384). Minimal scaffold avoids inventing a flexible generator. | Yes — could support custom milestone/slice IDs later |
| D036 | M001/S07 | pattern | Morning report as pure function | `generateMorningReport()` takes data in, returns string — no I/O in the formatter | Testable without mocks, reusable from both CLI and interactive contexts. Same pattern as formatCost/formatTokenCount. | No |
| D037 | M001/S07 | pattern | Metrics ledger access for report | Read `.gsd/metrics.json` directly instead of exporting loadLedger() | Avoids coupling morning-report to metrics.ts internals. The JSON format is stable (version 1). | Yes — export loadLedger if multiple consumers emerge |

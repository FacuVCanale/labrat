# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Starting point | GSD-2 codebase, not greenfield | GSD-2 has all the hard infrastructure (crash recovery, cost tracking, timeout, multi-provider LLM, state machine). Research logic is the new part. | No |
| D002 | M001 | arch | Hierarchy mapping | Campaign=Milestone, Phase=Slice, Experiment=Task | Inherit GSD-2's existing structure rather than inventing new abstractions. Same state machine, same file structure, same dispatch. | No |
| D003 | M001 | arch | Research flow semantics | Failure is data, phases are theories, advancement is exploration-driven | Fundamental difference from development flow. Failed experiments advance with knowledge, not block. | No |
| D004 | M001 | arch | MLOps integration strategy | Two-layer: Labrat logs orchestration via REST, eval scripts log domain metrics natively | User's eval scripts already live in Python where W&B/MLFlow SDKs are native. Labrat adds orchestration metadata via REST API. | Yes — if a TypeScript W&B SDK appears |
| D005 | M001 | pattern | Eval output format | JSON to stdout | Simple, parseable, language-agnostic. Eval prints {"metric": value} and Labrat parses it. | No |
| D006 | M001 | scope | Target file safety | Agent can only modify files explicitly specified by user | Eval script and infrastructure are immutable. Safety boundary prevents unintended modifications. | Yes — when multi-file experiments added in M002 |
| D007 | M001 | arch | Integration philosophy | Integrate existing tools, don't rebuild | Anything with a mature existing solution (MLOps tracking, visualization, statistical analysis) gets wired in, not reimplemented. Labrat's unique value is the autonomous research loop. | No |
| D008 | M001 | scope | LLM provider support | Keep full GSD-2 multi-provider support (20+) | Zero cost to maintain, inherited infrastructure. Users pick whatever model suits their research. | No |
| D009 | M001 | arch | Upstream tracking | Cherry-pick selective, not merge | Codebases will diverge. Cherry-pick infrastructure fixes from GSD-2, ignore development-specific changes. | No |
| D010 | M001/S01 | pattern | Integration branch | `dev` branch as integration target, not `main` | Slice branches branch from and merge into `dev`. `main` stays clean for releases. | No |
| D011 | M001/S01 | scope | Internal package naming | Keep `@gsd/*` workspace names and `gsd` extension directory unchanged | Renaming cascades into every import across 5 workspace packages for zero user-facing benefit. User-facing identity (package name, bin, env vars, config dir) is `labrat`. | Yes — if internal names cause confusion |
| D012 | M001/S02 | pattern | Campaign config format | `CAMPAIGN.json` in slice directory | JSON is parseable without markdown parsing complexity. Lives in slice directory because campaigns map to slices (D002). JSONL experiment log alongside it. | Yes — could switch to CAMPAIGN.md if markdown consistency matters |
| D013 | M001/S02 | pattern | Experiment log format | JSONL (one JSON object per line) | Crash-safe: truncated last line is recoverable. Append-only via appendFileSync. More robust than a single JSON array for crash scenarios. | No |
| D014 | M001/S02 | pattern | Experiment commit convention | `experiment(E001): description` / `revert(E001): discard — reason` | Greppable, follows conventional commit pattern. Distinct from development `chore()` commits. Enables git log filtering for experiment history. | No |
| D015 | M001/S02 | arch | Dual-mode state machine | Parallel research mode alongside existing development flow | `experimenting` phase added to Phase union, not replacing existing phases. Existing development flow untouched. Campaign config presence triggers research mode in deriveState(). | No |
| D016 | M001/S02/T01 | pattern | Campaign detection priority | Campaign config short-circuits deriveState before blocker detection | Active campaign takes priority over task-level blocker detection. Rationale: experiments are the work unit in research mode, not tasks. | Yes — may need refinement when experiments interact with task blockers |
| D017 | M001/S02/T01 | pattern | run-experiment artifact verification | Artifact path points to EXPERIMENT-LOG.jsonl existence, not individual entries | Auto-mode's retry/skip logic checks file existence. Entry-level validation is the eval runner's job (S03). | Yes — could add entry count check later |
| D018 | M001/S02/T02 | pattern | Dispatch guard for experiments | run-experiment added to SLICE_DISPATCH_TYPES (same sequential check as other slice work) | Experiments follow the same ordering constraint — can't dispatch until prior slices complete on main. Simpler than a custom bypass. | Yes — may need experiment-specific bypass if campaigns span multiple slices |
| D019 | M001/S02/T02 | pattern | commitExperiment failure mode | Throws on empty staging instead of silent no-op | Caller must know when nothing was committed. Silent no-ops hide bugs in experiment generation. | No |
| D020 | M001/S03/T01 | pattern | Metric parsing strategy | Bottom-up JSON scanning — last valid JSON object line wins | Eval tools commonly print progress/warnings then final metrics. Bottom-up finds the metrics line without requiring the eval to suppress other output. | No |
| D021 | M001/S03/T01 | pattern | Multi-run aggregation | Median per metric, not mean | Median is robust against outlier runs (one slow/fast run doesn't skew results). Standard practice for noisy benchmarks. | No |
| D022 | M001/S03/T01 | pattern | Zero baseline guard | Use divisor of 1 when baseline metric is 0, not epsilon | Epsilon (1e-10) produces astronomically large normalized scores that dominate composite scoring. Divisor of 1 produces meaningful relative scores. | No |
| D023 | M001/S03/T02 | pattern | Eval hook placement in handleAgentEnd | Between auto-commit and doctor/state-rebuild | Eval sees the committed code (post-commit). Doctor runs after any revert (post-eval). Correct ordering for both keep and discard paths. | No |
| D024 | M001/S03 | process | Do NOT write STATE.md in completion units | Skip STATE.md in slice completion — let auto-mode bookkeeping own that file | Auto-mode modifies STATE.md between auto-commit and branch checkout during squash-merge. If the completion unit also writes STATE.md, the file has uncommitted changes at checkout time and git aborts with "Your local changes would be overwritten." Broke the S03 merge. Completion units must not touch STATE.md. | No |
| D025 | M001/S04 | pattern | Missing target file degradation | Insert ⚠ placeholder + stderr warning instead of throwing | A missing file shouldn't crash an overnight run. LLM can still attempt modifications with remaining context. Consistent with resilient-by-default patterns (readAllExperiments skips corrupted lines, readBestMetrics returns null). | No |
| D026 | M001/S04 | pattern | Diff-stat timing in eval post-process | Extract diff-stat before any potential git revert | Revert changes HEAD, so `git diff --stat HEAD~1..HEAD` must be captured first. Applied to all three result construction sites in runExperimentPostProcess(). | No |
| D027 | M001/S05 | pattern | Lock enrichment strategy | Read-modify-write after writeLock, not changing writeLock signature | Preserves backward compatibility for all existing lock consumers. Only experiment dispatch path adds experimentNumber. | No |
| D028 | M001/S05 | pattern | Orphan commit detection | Git log --grep vs JSONL entry count | Relies on D014 commit message convention (`experiment(EYYY):`). Simple count comparison — more commits than log entries means orphan. | Yes — if commit convention changes |
| D029 | M001/S05 | pattern | Per-experiment budget comparison | Strict > for per-experiment, >= for campaign-level | At-budget is acceptable for a single experiment (noise margin). At-ceiling for the whole campaign is a hard stop. Different semantics, different operators. | Yes |
| D030 | M001/S05 | pattern | Budget guard failure semantics | Non-fatal try/catch — budget check failure never blocks dispatch | Budget is a safety valve, not a gate. If the check itself fails (ledger unavailable, format change), experiments continue rather than halting. | No |
| D031 | M001/S06 | pattern | MLOps circuit breaker | 5 consecutive failures → 5 min backoff, half-open probe, injectable nowFn for testing | Prevents hammering a down MLOps server while allowing automatic recovery. Constructor-injected for testability. | Yes — thresholds may need tuning |
| D032 | M001/S06 | pattern | Non-fatal MLOps hooks | All three lifecycle points (init/log/finish) wrapped in try/catch — MLOps failure never blocks experiments | MLOps is observability, not a gate. An overnight run must never fail because W&B is down. | No |
| D033 | M001/S06 | pattern | W&B auth encoding | Buffer.from base64 of "api:<key>" matching SDK wire protocol | Reverse-engineered from W&B Python SDK behavior. Undocumented but stable. | Yes — if W&B changes auth |
| D034 | M001/S07 | pattern | `labrat start` auto-trigger mechanism | Env var `LABRAT_AUTO_START=1` checked by session_start hook in GSD extension | Simple, ephemeral (process-scoped), no new APIs. Extension's session_start handler already has `pi` in scope for calling startAuto(). | Yes — could use a more formal hook if needed |
| D035 | M001/S07 | pattern | `labrat start` GSD scaffold | Creates minimal M001/S01 with roadmap, plan, and CAMPAIGN.json — just enough for deriveState() | deriveState() requires slice plan to be truthy before campaign detection fires (line 384). Minimal scaffold avoids inventing a flexible generator. | Yes — could support custom milestone/slice IDs later |
| D036 | M001/S07 | pattern | Morning report as pure function | `generateMorningReport()` takes data in, returns string — no I/O in the formatter | Testable without mocks, reusable from both CLI and interactive contexts. Same pattern as formatCost/formatTokenCount. | No |
| D037 | M001/S07 | pattern | Metrics ledger access for report | Read `.gsd/metrics.json` directly instead of exporting loadLedger() | Avoids coupling morning-report to metrics.ts internals. The JSON format is stable (version 1). | Yes — export loadLedger if multiple consumers emerge |
| D038 | M002 | arch | Simplicity scoring approach | Diff-stat based (lines added/removed from `extractDiffStat`) as primary signal, with `simplicityWeight` defaulting to 0 (disabled). Not AST-based. | Language-agnostic, already extracted per experiment, avoids rabbit hole of AST parsers for arbitrary languages. LLM judgment considered but rejected for non-determinism in the keep/discard critical path. | Yes — could add LLM tie-breaker later |
| D039 | M002 | arch | Module extraction for M002 features | New features as separate .ts modules (simplicity-scorer.ts, agenda.ts, steering.ts). auto.ts remains orchestrator calling into modules. | auto.ts is 3250 lines — at the limit of maintainability. Following the pattern set by eval-runner.ts, mlops-integration.ts, morning-report.ts. | No |
| D040 | M002 | arch | Sequential phases, not DAG | Experiment sequencing uses simple ordered phases (phase 1 completes → reassess → phase 2 starts), not a dependency graph | Sequential phases cover 90% of use cases. DAG scheduling adds complexity (cycle detection, partial completion semantics) with minimal incremental value. | Yes — build DAG only if sequential proves insufficient |
| D041 | M002 | pattern | Atomic steering file writes | Write-to-temp-then-rename pattern for STEERING.json — atomic on POSIX | Two processes (labrat auto + labrat discuss) access the same file. Node.js writeFile is not atomic; partial writes produce invalid JSON. Temp+rename is the standard POSIX solution. | No |
| D042 | M002 | pattern | CampaignConfig backward compatibility | All new fields (simplicityWeight, agenda) are optional with sensible defaults. M001-era configs parse identically. | Breaking existing campaigns would break M001's proven core loop. parseCampaignConfig() validates shape explicitly — new optional fields are additive. | No |
| D043 | M002 | pattern | Target file validation strategy | Post-commit `git diff --name-only HEAD~1..HEAD` checked against targetFiles[]. Violations trigger immediate revert without running eval. | smartStage() stages ALL changes (not just target files). Prompt-enforced safety (D006) is necessary but not sufficient. Post-commit validation catches LLM violations before wasting eval time. | No |
| D044 | M002/S02 | arch | Phase transitions in dispatchNextUnit, not deriveState | Phase boundary detection and advance happens in `dispatchNextUnit` reading `AGENDA-STATE.json`. `deriveState` unchanged — `maxExperiments` remains the only campaign termination trigger. | deriveState is 560 lines with 33 contract tests. Adding conditional paths for agenda phases risks subtle state bugs for non-agenda campaigns. Phase logic in dispatchNextUnit is isolated and testable without touching the state derivation critical path. | Yes — could move to deriveState if phase state needs to be visible to other consumers |
| D045 | M002/S02 | pattern | Atomic AGENDA-STATE.json writes | Write-to-temp-then-rename for AGENDA-STATE.json, matching D041 pattern for STEERING.json | AGENDA-STATE.json is written at phase boundaries and must survive process crash. Corrupt state recovers to phase 0 (safe default). Same atomic write pattern used consistently across M002 state files. | No |
| D046 | M002/S02 | pattern | Phase-scoped metrics and history as agenda.ts functions | `getPhaseBestMetrics` and `getPhaseFilteredHistory` live in agenda.ts, not eval-runner.ts | Keeps phase-awareness contained in the agenda module. eval-runner.ts functions (`readBestMetrics`, `readAllExperiments`) remain unchanged — agenda.ts wraps them with phase-scoping. Avoids growing eval-runner.ts or adding agenda coupling. | No |
| D047 | M002/S02/T02 | pattern | Facade functions in agenda.ts for auto.ts call sites | `checkAndAdvancePhase`, `getPhasePromptOverrides`, `stampPhaseIndex` — multi-step operations packaged as single calls | auto.ts stays thin (3269 lines, 19 net delta from 3250 baseline). All agenda logic in agenda.ts, auto.ts gets 3 import + 3 call sites. Prevents auto.ts from becoming a multi-concern monolith. | No |
| D048 | M002/S03 | scope | Steering directive types for S03 | `refocus`, `skip_phase`, `stop` — `add_experiments` deferred | `add_experiments` has ambiguous semantics (does it modify CAMPAIGN.json? current phase only?). The three shipped types cover core steering value. Defer until user demand clarifies semantics. | Yes — add `add_experiments` if users need it |
| D049 | M002/S03 | pattern | Persistent refocus context via STEERING-FOCUS.md | `refocus` directive writes STEERING-FOCUS.md in slice dir — read by `getSteeringPromptOverride` for prompt injection across subsequent experiments | STEERING.json is consumed-then-deleted (spec requirement). Refocus needs to persist across experiments. Separate file is transparent (`cat STEERING-FOCUS.md`), decoupled from AGENDA-STATE.json (works for non-agenda campaigns too). Cleared on phase boundary (new phase = fresh direction). | Yes — could move into AGENDA-STATE.json if coupling is acceptable |
| D050 | M002/S03 | pattern | Discuss command routing via deriveState phase check | `showDiscuss` checks `state.phase === "experimenting"` and routes to `showSteering`. Works cross-process because deriveState reads disk state. | No new subcommand needed. Routing is transparent — campaign active means steering, no campaign means normal discuss. Cross-process works because disk state is the source of truth, not process-local variables. | Yes — could add explicit `/gsd steer` subcommand if routing causes confusion |
| D051 | M002/S03 | pattern | LLM-assisted steering via gsd-steer workflow | `showSteering` dispatches `steer-campaign.md` prompt as `gsd-steer` workflow — LLM helps formulate directive and writes STEERING.json | Same pattern as showPlan/showDiscuss. LLM has campaign context to help user formulate effective directives. The alternative (raw ask_user_questions) lacks campaign awareness and can't access the file system to write STEERING.json. | Yes — could add direct non-LLM path for fast/cheap steering |
| D052 | M003 | arch | Three-phase sync pipeline | Fetch/categorize → apply/verify → LLM-adapt, each as a separate slice | Each phase is independently valuable and has a different risk profile. Phase 1 (report) is the primary user workflow — visibility before action. Phase 2 (apply) handles the easy cases. Phase 3 (LLM adapt) handles the hard cases. Dependency order matches risk retirement: classification accuracy → apply mechanics → adaptation quality. | No |
| D053 | M003 | pattern | File-path-primary classification for upstream commits | File paths as primary signal (`packages/*` → infrastructure, known Labrat-added files → skip), commit message prefixes as secondary | Commit messages like "refactor: PR quality fixes" touch both infrastructure and dev-specific code. File paths are deterministic and auditable. Per-file analysis for the 12 overlapping GSD extension files resolves mixed commits. | Yes — could add ML-based classification later |
| D054 | M003 | pattern | UPSTREAM-SYNC.json for persistent sync state | Atomic state file tracking last evaluated commit, evaluated commit list, applied commits | Without persistent tracking, the sync report re-shows old commits every time. Same atomic write pattern (D041/D045) as AGENDA-STATE.json and STEERING.json. Version field for future migration. | No |
| D055 | M003 | scope | Sync module fully decoupled from experiment loop | upstream-sync.ts has no imports from auto.ts, eval-runner.ts, or campaign lifecycle | Sync is an operational tool, not a research loop component. A sync in progress must never interfere with `labrat auto`. The module uses git-service.ts for git operations but is otherwise standalone. | No |
| D056 | M003/S01 | pattern | Conflict detection graceful degradation on unrelated histories | `getConflictFiles()` returns empty set when no merge-base exists between HEAD and upstream/main | Repos forked via independent init (not git fork) have unrelated histories. Crashing on `git merge-base` failure would block the entire sync report. Empty conflict set is conservative — S02 uses actual cherry-pick dry-run for accuracy. | No |
| D057 | M003/S01 | pattern | Sync CLI persists ALL fetched commits as evaluated | `writeSyncState()` saves every commit from `fetchUpstreamCommits()`, not just those shown after filtering | Ensures `--include-evaluated` can always re-show the full set. Prevents re-evaluation drift where filtered commits would be re-fetched on the next run. Consistent with "evaluate once, show on demand" semantics. | No |
| D058 | M003/S01 | pattern | Conservative classification default for unknown files | Files not matching any known pattern default to `infrastructure` category | Safer to surface an irrelevant upstream commit than to hide a relevant one. Users can ignore false positives but can't act on false negatives they never see. | Yes — could refine with more patterns |
| D059 | M003/S02 | impl | Git commit messages via stdin | Use `git commit -F -` with stdin pipe instead of `-m` flag | Commit messages with parentheses/special chars break shell parsing when passed via `-m`. `-F -` with piped input is safe for all content. Consistent with existing GitServiceImpl pattern. | No |
| D060 | M003/S03 | pattern | Multi-format FILE header parsing in LLM output | `parseAdaptedFiles` supports three header variants: `// FILE: path`, `## FILE: path`, `**FILE: path**` | LLMs drift on formatting conventions. Supporting three common variants (code comment, markdown heading, bold) makes parsing robust without being fragile to prompt adherence. Single `extractFilePath` helper handles all three. | Yes — extend if new header formats appear |
| D061 | M003/S03 | pattern | Adapted commit message convention | `upstream-adapt(<short-hash>): <subject>` distinct from `upstream-sync(<short-hash>): <subject>` | Adapted commits (LLM-resolved conflicts) are mechanically different from clean cherry-picks. Distinct prefix enables `git log --grep` filtering for auditing which upstream changes required adaptation vs. applied cleanly. | No |
| D062 | M004 | arch | Compute backend abstraction scope | Only eval command dispatches to remote; orchestration/git/state stay local | LLM edits files locally, git commits locally, diff-stat/validation run locally. Only the eval subprocess gets rerouted. Keeps the abstraction surface minimal and the orchestrator unchanged. | No |
| D063 | M004 | arch | Code sync mechanism | Git push/pull for all remote backends | Same transport for SSH and Docker. Push experiment branch to origin, remote pulls. No rsync/scp/zip. Reuses existing git infrastructure. | Yes — if a backend needs non-git sync |
| D064 | M004 | arch | Native CLI over Node.js libraries | Use native `ssh` and `docker` binaries via child_process, not Node.js client libraries | Zero new dependencies. Leverages user's existing SSH config, keys, Docker config. Same pattern as existing `spawnSync` for git. | No |
| D065 | M004 | scope | Shipped backends: SSH + Docker | SSH and Docker ship first; Modal/RunPod/Lambda/K8s deferred | SSH covers every GPU box/VM. Docker covers containerized reproducibility. Interface designed for future backends. | Yes — add backends when demand emerges |
| D066 | M004 | arch | Backend failure → discard decision | Remote backend errors produce experiment discard with error message, not unhandled exceptions | Overnight runs must survive remote failures. Same pattern as eval timeout/crash handling in M001. | No |
| D067 | M004/S01 | pattern | LocalBackend replicates spawnSync logic, does not delegate to runEval() | LocalBackend has its own spawnSync call rather than calling the existing runEval() function | Future backends (SSH, Docker) won't use runEval(). Delegating would couple the abstraction to a convenience function. runEval() stays exported unchanged for backward compat with tests. | No |
| D068 | M004/S02 | pattern | Pre-push remote ref check via ls-remote | Check remote ref before push to distinguish already-up-to-date from needing push | Avoids redundant push + parsing 'Everything up-to-date' from git stderr. Clean semantic: remote matches local HEAD → no-op return, no side effects. | No |
| D069 | M004/S02 | pattern | Post-push ls-remote verification | After push, verify remote ref matches local HEAD via ls-remote instead of trusting push exit code | Push exit code 0 doesn't guarantee the remote ref was updated (e.g., hook failures, partial pushes). ls-remote verification provides auditable proof. | No |
| D070 | M004/S04 | pattern | Docker safety-net timeout +60s | Safety-net spawnSync timeout set to timeoutSecs + 60s (vs SSH's +30s) | Docker has additional startup overhead: image pull, container creation, filesystem setup. 60s buffer prevents false safety-net kills during legitimate Docker startup. | Yes — tune if image pulls are very slow |
| D071 | M004/S04 | pattern | Remote Docker checkout by ref, not branch name | Remote path captures HEAD ref after push, uses `git checkout <ref>` not `git checkout <branch>` | Branch name could advance between push and container startup (another push from another process). Ref is deterministic — the container gets exactly the code that was pushed. | No |
| D072 | M004/S04 | pattern | Mock binary on PATH for Docker contract tests | Shell script prepended to PATH captures args and returns configurable exit/stdout/stderr | Tests the real spawnSync('docker', ...) call path including argument assembly. Module-level mocking would skip argument construction logic, which is where Docker bugs live. Same pattern as ssh-backend.test.ts. | No |
| D073 | M004/S05 | pattern | Compute config validation at parse time, not at resolve time | `validateComputeConfig()` called inside `parseCampaignConfig()` — invalid compute config returns null (same as malformed required fields) | Catches typos/missing fields at config load, before any eval attempt. Consistent with existing validation pattern. `resolveBackend()` continues to trust its input — validation is upstream. | No |
| D074 | M004/S05 | pattern | Backend error wrapping in eval-runner, not auto.ts | Try/catch around resolveBackend + eval loop in `runExperimentPostProcess()` produces discard ExperimentResult | All callers benefit (not just auto.ts). Error is indistinguishable from eval failure — same revert + JSONL logging. auto.ts catch at line 647 remains as a safety net. | No |
| D075 | M004/S05 | pattern | Pre-flight checks are non-blocking and skippable | `checkSSHConnectivity` / `checkDockerDaemon` return structured result, never throw. If pre-flight itself errors, skip it and let actual eval surface the problem. | Pre-flight is optimization — catches errors early with actionable messages. Must never block a working setup due to its own failure. | No |
| D076 | M005 | arch | Leverage GSD's existing 4-agent flow for hypotheses | Hypothesis = slice (research-slice once, then experiment tasks), Experiment = task (plan→execute→verify per task) | GSD already implements research→plan→execute→verify as separate agents with fresh context. Reusing this avoids building a new state machine. | No |
| D077 | M005 | scope | User-facing naming only for NightShift cleanup | Replace GSD/labrat in prompts, CLI output, README, examples. Leave internal identifiers (src/resources/extensions/gsd/, @gsd/ imports) alone. | Renaming internal paths touches every import and risks breakage across 40+ files for zero user-visible benefit. | No |
| D078 | M005 | convention | Hypothesis-native terminology | Milestone=session, slice=hypothesis, task=experiment in user-facing surfaces and data structures | Aligns the flow's mental model with research semantics. The researcher discovers hypothesis content; experiments refine within a hypothesis. | No |
| D079 | M005 | pattern | Full research every experiment's hypothesis, not just once at campaign start | Research agent uses full tool suite (web search, library docs, fetch_page) for each hypothesis, not a single initial pass | User's strongest constraint: "never feel like shallow research theater." The agent has no domain skills — research is what makes hypotheses informed. | No |
| D080 | M005 | pattern | Verifier produces structured analysis feeding next experiment | Verify agent outputs what worked, what didn't, what to signal to next experiment's plan agent | Creates learning loop between experiments. Raw metrics alone don't explain WHY results happened. | No |
| D081 | M005/S01 | impl | ASCII logo style | Block-character NIGHTSHIFT at 77 chars wide (fits 80-col terminals) rather than simplified plain text | Matches original LABRAT logo aesthetic. 77 chars ensures no wrapping in standard terminals. | No |
| D082 | M005/S01 | scope | Historical GSD references in README | 3 "The original GSD..." sentences kept as predecessor proper nouns, not renamed | GSD is the real name of the predecessor project. Renaming historical references would be revisionist. | No |
| D083 | M005/S01 | scope | Test fixture GSD data unchanged | files.test.ts and parsers.test.ts GSD strings are arbitrary fixture data, not generated output assertions | Changing test fixtures that happen to contain "GSD" as arbitrary data would be churn with no user-facing benefit. Only assertions on actual output strings were updated. | No |
| D084 | M005/S03 | pattern | /nightshift as programmatic wizard, not LLM discussion | `showNightShiftInterview` uses `ctx.ui.input()` and `ctx.ui.select()` loop, not `dispatchWorkflow()` with an LLM | Interview collects 6-7 structured fields — deterministic wizard is faster, cheaper, and matches user preference for quick-start. LLM discussion adds latency and token cost for no benefit when questions are known upfront. | Yes — could add LLM-driven interview later |
| D085 | M005/S03 | pattern | Scaffold generator as pure function | `generateNightShiftScaffold(basePath, config)` takes structured input, writes files — no I/O beyond fs writes to basePath | Testable without mocking ctx.ui or LLM. Contract tests can call it directly against parsers. Same pattern as `generateMorningReport()` (D036). | No |
| D086 | M005/S03 | pattern | Priors stored in both CAMPAIGN.json and PRIORS.md | `priors` field added to CampaignConfig (available to buildExperimentPrompt immediately) + PRIORS.md in slice dir (for richer context injection by S04) | Short priors work in JSON; PRIORS.md handles long-form text. Both paths available to downstream consumers without requiring S04 changes for basic priors injection. | Yes — could consolidate to one location |
| D087 | M005/S04 | pattern | readLatestExperimentAnalysis scans backwards from current experiment | `readLatestExperimentAnalysis(sliceDir, currentExperimentNumber)` loops from N-1 down to 1 looking for EXPERIMENT-NNN-ANALYSIS.md | Simple, no directory listing needed. Matches the sequential experiment numbering contract. Falls back to placeholder string when no prior analysis exists. | Yes — could cache or use glob if analysis files become sparse |
| D088 | M005/S04 | pattern | Execute builder takes experimentPlan as explicit parameter | `buildExecuteExperimentPrompt` receives the plan text as a string parameter rather than reading it from disk | The plan agent's output lives in LLM context, not a persisted file. Passing it explicitly keeps the builder pure and avoids inventing a new file format. S05 dispatch will pass the plan output directly. | Yes — could persist plans to disk if needed for debugging |
